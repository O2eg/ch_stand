api_version: ch_stand/v1
kind: ClickHouseStand

metadata:
  name: ch-stand-replica-pair

spec:
  clickhouse:
    version: "25.8.28.1"
    cluster_name: ch_stand_replica_pair
    database: default
    user: default
    settings:
      max_threads: 4
      log_queries: true
      log_query_threads: true

  topology:
    shards: 1
    replicas: 2
    keeper_nodes: 3

  docker:
    pull_policy: missing
    labels:
      io.ch-stand.profile: replica-pair

  storage:
    root_directory: .ch_stand/ch-stand-replica-pair

  ports:
    bind_address: 127.0.0.1
    http_base: 18130
    native_base: 19010
    ssh_base: 12230
    keeper_base: 19181

  resources:
    server:
      cpu_limit: 1.0
      memory_limit: 2g
      shm_size: 256m
    keeper:
      cpu_limit: 0.5
      memory_limit: 512m
      shm_size: 128m

  diagnostics:
    perf: true
